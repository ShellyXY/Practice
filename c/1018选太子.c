#include<stdio.h>
#include<string.h>

int main()
{
        int used[1000];
        int count;
        int left[1000];
        int out;
        int i;
        int peoplenum,sound;

        while(scanf("%d%d",&peoplenum,&sound)!=EOF)
        {
            peoplenum*=2;
            memset(used,0,sizeof(int)*100);
        }
}
